# kit-tech-local

Local mirror of https://kit-tech-51044992.figma.site

Open `index.html` in a browser. If the browser blocks some Figma runtime fetches from `file://`, run a simple local server from this folder:

ruby -run -e httpd . -p 8080

Then open http://localhost:8080
